# Dependencies
node_modules/

# Next.js
.next/
out/

# Environment variables — NEVER commit these
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
dax-agent/.env

# Python ADK agent (dax-agent/)
__pycache__/
*.pyc
.venv/
dax-agent/.venv/
# Keep uv.lock committed for reproducible installs — do NOT ignore it.

# Service account keys — NEVER commit these
*serviceaccount*.json
*service-account*.json

# Claude Code — keep settings local, not in version control
.claude/settings.json
.claude/settings.local.json

# Firebase
.firebase/
firebase-debug.log

# OS
.DS_Store
Thumbs.db

# Build output
dist/
build/